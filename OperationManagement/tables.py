import django_tables2 as tables
from django import  django_tables2 as tables
from .models import RegistoMovimento

# class PersonTable(tables.Table):
#     class Meta:
#         model = Person
#         sequence = ("last_name", "first_name", "birth_date", )
#         exclude = ("user", )